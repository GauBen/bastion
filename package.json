{
  "name": "bastion",
  "version": "0.0.1",
  "dependencies": {
    "@prisma/client": "^3.10.0",
    "dotenv": "^16.0.0",
    "nanoid": "^3.3.1",
    "prisma": "^3.10.0"
  },
  "main": "run.js",
  "packageManager": "pnpm@6.32.1",
  "private": true,
  "scripts": {
    "build": "pnpm run build --recursive",
    "clean": "pnpm run clean --recursive",
    "dev": "NODE_ENV=development pnpm run dev --recursive",
    "lint": "pnpm run lint --recursive",
    "start": "NODE_ENV=production node ."
  },
  "type": "module",
  "volta": {
    "node": "16.14.0"
  }
}
